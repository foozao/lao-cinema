# Video Server Configuration

# Server
PORT=3002
HOST=0.0.0.0

# API URL for token validation
# Video server validates tokens by calling the API instead of duplicating logic
API_URL=http://localhost:3001

# Video Security
# Secret for session cookies (local verification for HLS segment requests)
# Generate with: openssl rand -hex 32
VIDEO_TOKEN_SECRET=dev-secret-change-in-production
