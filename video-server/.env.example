# Video Server Configuration

# Server
PORT=3002
HOST=0.0.0.0

# Video Security
# Secret for validating video access tokens (must match API server)
# Generate with: openssl rand -hex 32
VIDEO_TOKEN_SECRET=dev-secret-change-in-production
