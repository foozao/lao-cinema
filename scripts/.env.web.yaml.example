# Web app environment variables for Cloud Run deployment
# Copy this file to .env.web.yaml and update with your actual passwords
# Format: "username:password:role,username2:password2:role2"
# Roles: admin (full access) or viewer (no admin pages)
AUTH_USERS: "admin:YourAdminPassword:admin,test:YourTestPassword:viewer"
