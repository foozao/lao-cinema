# TMDB API Configuration
# Get your API key from: https://www.themoviedb.org/settings/api
# Copy this file to .env and add your actual API key

# Option 1: Server-side only (RECOMMENDED - more secure)
# API key is not exposed to the browser
TMDB_API_KEY=your_tmdb_api_key_here

# Option 2: Client-side (less secure, but works without backend)
# API key will be visible in browser Network tab
# NEXT_PUBLIC_TMDB_API_KEY=your_tmdb_api_key_here

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Site URL (used for SEO, emails, OAuth callbacks, etc.)
# Alpha/Beta: https://laocinema.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Sentry Error Monitoring (production only)
# Get DSN from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
NEXT_PUBLIC_SENTRY_DSN=
# For source map uploads (CI/CD only)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
